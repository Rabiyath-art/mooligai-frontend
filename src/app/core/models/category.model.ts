export interface Category {
    // _id: string;
    // name: string;
    // slug: string;
    // isActive?: boolean;

        _id: string;

    name: string;

    slug: string;

    image?: string;

    description?: string;

    isActive: boolean;

    createdAt: string;

    updatedAt: string;
}